# 1. 어떤 내용이 가장 인상적이었나요?
- S3 저장 비용이 비효율적이고 비싸다는 것을 알게 되었다. 단기/일회성 환경에서는 AWS 인스턴스의 로컬 디스크 사용해야겠다.
- 실제로 기존의 연구는 fetcher 전용 환경이었지만 저자는 로컬 환경에서 우수한 성능을 냈다.
- 몰랐는데, NVMe SSD 디스크 속도가 거의 RAM I/O만큼 대역폭이 따라왔다는 사실을 알게 되었다. 학교에서 공부할 때는 항상 디스크 I/O가 RAM I/O에 비해 10배 정도 느리다고 공부해왔는데, 역시 기술이 빠른 속도로 발전하고 있다는 점을 알았다.
- **크롤링을 할 때 웹 예의를 준수해야한다는 점을 알았다. 예를 들어** robots.txt 준수. 사용자 에이전트에 연락처 포함, 가정용 사이트는 피하고, 도메인당 최소 70초 지연 유지하는 등 조건을 걸어야한다. 과도한 크롤링은 서버 관리자에게 문제를 일으킬 수 있다. 사실 크롤링할 때 이런 예의 부분은 전혀 생각을 못하고 있었는데, 웹 크롤링에도 에티켓이 있다는 것을 깨달았다.

# 2. 아키텍처에 대해 논의해보세요.
- 전통적인 크롤러가 fetcher, parser, coordinator, scheduler, storage별로 분리해서 수평 확장하는 방식
    - fetcher : 웹페이지 다운로드, URL을 받아 실제 HTTP 요청을 보내고, 응답 HTML 또는 리소스를 받아오는 역할.
    - Parser : fetcher가 받아온 HTML을 분석하여 `<a href="">` 링크를 추출하거나, `<title>` 등의 메타데이터를 분석.
    - Scheduler : Frontier(크롤 대기 URL 목록)를 관리하고, 어떤 URL을 언제 fetcher에게 줄지를 결정. 도메인별 요청간 70초 대기 등을 수행
    - coordinator : 전체 시스템 흐름 제어, 여러 fetcher, parser, scheduler 간의 **조율과 모니터링**.
    - Storage : 수집한 HTML, 메타데이터, 추출된 링크 등을 디스크, DB, 또는 클라우드 저장소(S3 등)에 저장.
- 분산 클러스터 ; **각 노드에 모든 기능을 통합**해서 독립적으로 동작하게 하고, 이를 수평 확장한 구조
- 자동차를 만드는 공장이 있다고 할 때, 기존의 방식은 부품 공장에서 부품 만들고, 그 부품을 가지고 제조 공장에서 차체를 만들고, 색칠 공장에서 색칠
- 현재 제시된 아키텍처는 하나의 공장에서 자동차 하나를 다 만들고, 그러한 공장을 여러 개 만드는 것에 비유할 수 있을 것 같다.

# 3. 어떤 의사결정이 놀라웠나요?
- 먼저 작은 실험부터 시작하여, 노드를 늘리며 성능을 확장해나가고, hot shard 문제 (야후와 같이 특정 인기있는 도메인이 할당된 노드만 사용량이 큰 문제) 가 생긴 이유까지 시각화와 통계분석을 통해 알아낸 점이 인상깊었다.
- 일반적으로는 병목 해소, 확장성, 유지보수성 때문에 기능을 나누지만, 이 저자는 **"단일 머신 최적화"에 집중한 후 수평 확장으로 넓힌 점이 인상깊었다.**
